reset()
setStep(10)

jump(getWidth() / 2, getHeight() / 2)

setBackground('black')
string[] colors = ['red', 'orange', 'yellow', 'green', 'aqua', 'blue', 'purple']

for number i = 0 to 500
{
	setColor(colors[i % size(colors)])
	move(i * 0.1)
	rotate(15)
}